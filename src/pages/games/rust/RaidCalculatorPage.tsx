import { GameOptionPlaceholder } from '../shared/GameOptionPlaceholder'

export function RaidCalculatorPage() {
  return (
    <GameOptionPlaceholder
      gameName="Rust"
      optionName="Calculadora de raideos"
    />
  )
}
